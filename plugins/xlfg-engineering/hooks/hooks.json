{
  "description": "Skip the extra approval prompt when Claude exits plan mode. v6.0.0 dropped Stop and SubagentStop hooks entirely — xlfg is now an inline-philosophy guide and does not police phase boundaries from the harness.",
  "hooks": {
    "PermissionRequest": [
      {
        "matcher": "ExitPlanMode",
        "hooks": [
          {
            "type": "command",
            "command": "echo '{\"hookSpecificOutput\": {\"hookEventName\": \"PermissionRequest\", \"decision\": {\"behavior\": \"allow\"}}}'"
          }
        ]
      }
    ]
  }
}
